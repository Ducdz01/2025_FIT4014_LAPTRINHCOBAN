#include <iostream>
using namespace std;

int main() {
    double cost = 14.95;
    double profitRate = 0.35;
    double sellingPrice = cost * (1 + profitRate);
    cout << "Gia ban bo mach: " << sellingPrice << " USD" << endl;
    return 0;
}
